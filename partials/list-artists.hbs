<!-- list-artists.hbs -->
<header class="category-title flex row gap-x1" tabindex="0">
    <h3 title="{{heading}}" role="heading" aria-level="3">{{heading}}</h3>
</header>
{{#if names-only}}
{{#get "posts" include="tags,authors" filter="primary_tag:{{section}}" limit="all" as |post|}}
<article class="list-artists-names flex">
    <div class="flex row wrap start start-items gap-x2">
        {{#foreach post}}
        <article class="category-item magic" aria-label="{{title}}" title="{{title}}" tabindex="0">
            <header class="item-title">
                <h4 role="heading" aria-level="4"><a href="{{url}}" title="{{title}}" target="_self">{{title}}</a></h4>
            </header>
        </article>
        {{/foreach}}
    </div>
</article>
{{/get}}
{{else}}
{{#get "posts" include="tags,authors" filter="primary_tag:{{section}}" limit="all" as |post|}}
<article class="category-content flex">
    <div class="flex row wrap start start-items gap-x2-5">
        {{#foreach post}}
        <article class="category-item magic" aria-label="{{title}}" title="{{title}}" tabindex="0">
            <figure class="item-figure" title="{{#if feature_image_alt}}{{feature_image_alt}}{{else}}{{title}}{{/if}}">
                <img src="{{img_url feature_image}}" class="item-img" alt="{{#if feature_image_alt}}{{feature_image_alt}}{{else}}{{title}}{{/if}}" />
            </figure>
            <header class="item-title">
                <h4 role="heading" aria-level="4"><a href="{{url}}" title="{{title}}" target="_self">{{title}}</a></h4>
            </header>
        </article>
        {{/foreach}}
    </div>
</article>
{{/get}}
{{/if}}
